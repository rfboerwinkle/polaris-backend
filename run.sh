#!/usr/bin/sh

python main.py

# eventually, we will want to run the engine, and hook it up with pipes.

# We will also want to start the oracle database daemon presumably? Does it have one of those?
docker compose up -d